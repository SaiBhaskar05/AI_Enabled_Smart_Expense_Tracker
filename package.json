{
  "name": "ai-enabled-smart-expense-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0 <=22.x"
  }
}
